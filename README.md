# Scaling Risk Checker MVP

Simple decision engine to detect scaling risk in e-commerce businesses.

## Run locally

python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
streamlit run app.py
